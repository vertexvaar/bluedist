# yaml-language-server: $schema=../../../vendor/symfony/dependency-injection/Loader/schema/services.schema.json
services:
  _defaults:
    autowire: true
    autoconfigure: true

  VerteXVaaR\BlueSeed\:
    resource: '../src/*'

  VerteXVaaR\BlueSeed\SeedService:
    public: true
    arguments:
      $seeders: !tagged_iterator app.seeder
