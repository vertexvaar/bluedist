{# @var element \VerteXVaaR\BlueForm\Element\Form\Input #}

<input id="{{ element.name }}"
       type="hidden"
       name="{{ element.htmlPathString }}"
       value="{{ element.value }}"
       autocomplete="off"
>
